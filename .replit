run = "git pull --quiet || true && pip install -r requirements.txt && python -m src serve --port 3000"

[deployment]
run = ["sh", "-c", "git pull --quiet || true && pip install -r requirements.txt && python -m src serve --port 3000"]
visibility = "private"

[[ports]]
localPort = 3000
externalPort = 80
